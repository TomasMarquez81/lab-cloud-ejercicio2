// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://tomasmarquez81.github.io',
    base: '/lab-cloud-ejercicio2',
    build: {
        assets: 'css'
    }
});
